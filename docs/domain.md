# 领域约定

电影放映授权编排库围绕影片拷贝、字幕、授权和放映发布保存业务事实。主要对象为影片、拷贝、字幕版本、授权合同、场次、借运交接、研究访问、发布游标。

必须长期遵守以下约束：发布时原子校验授权、位置、字幕和场地；授权修订产生影响事件；迟到交接按发生时间重建位置；下游按游标续传。

时间以带时区的 ISO 8601 字符串交换，服务内部保存 UTC；外部写请求使用调用方提供的幂等键；审计记录采用追加方式保存，不通过覆盖来隐藏历史。